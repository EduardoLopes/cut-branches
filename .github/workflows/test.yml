name: 'test'

on:
  workflow_call:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: node-setup
        uses: ./.github/workflows/node-setup.yaml

      - name: Run Tests
        run: pnpm test
