---
'cut-branches': patch
---

Rework the commit card layout: the branch/history icon now flanks the commit
message, the subject reads as a heading, and author · relative date share a
single trailing meta line.
