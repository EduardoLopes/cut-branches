---
'cut-branches': patch
---

Fixed the repository, diff, history, cleanup and settings pages overlapping the footer: they sized themselves from the viewport rather than from the space the app shell actually gives them.
