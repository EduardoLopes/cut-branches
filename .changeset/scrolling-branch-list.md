---
'cut-branches': minor
---

Replace the branch list's pagination with a single virtualized, infinitely-scrolling list — rows load as you scroll, stay mounted once passed, and the scroll port is keyboard-focusable.
