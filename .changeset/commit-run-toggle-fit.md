---
'cut-branches': patch
---

Fix the commit history run toggle overflowing its gutter column: it is now a borderless ghost button that fits the column exactly, and the run divider strip's icon no longer oversizes.
