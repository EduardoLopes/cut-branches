---
'cut-branches': minor
---

On macOS the sidebar collapse toggle now sits in the titlebar next to the traffic lights, so it stays reachable at the same spot whether the sidebar is open or collapsed to its rail. Windows and Linux keep it in the sidebar's own bottom row.
