---
'cut-branches': patch
---

Refresh the Rust and frontend dependency stack to the latest releases (git2 0.21, tree-sitter 0.27, notify 8, Tauri plugins, SvelteKit 2.70, Svelte 5.57), so the app ships with the newest upstream fixes.
