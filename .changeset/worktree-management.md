---
'cut-branches': minor
---

Add worktree management: a flag-gated worktrees view for a repository that lists, adds, removes, and locks git worktrees, with a Branches/Worktrees context switch in the repository header.
