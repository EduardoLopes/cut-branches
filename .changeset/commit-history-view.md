---
'cut-branches': minor
---

Add a flag-gated commit-history view with an interactive branch graph. Open it from a repository's menu to browse the full commit log across branches, see each branch's ahead/behind comparison against the base, preview a branch's graph on hover from the branch list, and select branches to delete directly from the graph.
