---
'cut-branches': minor
---

Split diff layout now always wraps long lines — half-width columns made horizontal scrolling unusable. The "Wrap lines" option shows as on and locked while split is active and still applies when switching back to unified.
