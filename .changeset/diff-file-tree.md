---
'cut-branches': minor
---

The diff view gains a file tree: changed files grouped by directory (deep single-child paths compressed) in a pane beside the list — activating a file scrolls to its diff and opens it.
