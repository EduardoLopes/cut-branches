---
'cut-branches': patch
---

Refine the current branch in the branch list: its card now fills the full row width and is marked with a "current" badge. The current branch is no longer lockable, since it already can't be deleted.
