---
'cut-branches': minor
---

Branch commit cards now show the commit's short SHA and, when configured, its upstream tracking ref, and can disclose the full commit message body via a per-card toggle. The backend now stores and exposes both the commit subject (summary) and the full message, plus each branch's upstream.
