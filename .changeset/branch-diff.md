---
'cut-branches': minor
---

Add a branch/commit diff review view — a changed-files list with expandable, syntax-highlighted diffs — with entry points from branch cards, commit rows, and the delete-branch confirmation. Gated behind the `branch-diff` feature flag (off by default).
