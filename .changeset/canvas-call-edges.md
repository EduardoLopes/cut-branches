---
'cut-branches': minor
---

Render symbol-level call edges on the diff canvas: edges now show which symbols one file uses from another, drawn as colored, multi-line labelled connections with wider node spacing so the relationships stay readable.
